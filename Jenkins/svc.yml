apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app:  my-service
spec:
  selector:
    app: dietpred
  type: NodePort
  ports:
    - nodePort: 80
      port: 80
      targetPort: 80
